version: "3.8"
services:
  mesh-organiser:
    build: .
    ports:
      - "9435:9435"
    volumes:
      - ./data:/app/data
      - ./config:/app/config
    environment:
      - NODE_ENV=production
      - VITE_API_PLATFORM=web
    restart: unless-stopped
